# AI Fitness Coach Bot - Техническая документация

## 📋 Обзор проекта

AI-помощник для фитнес-коучинга в Telegram с персонализированными тренировками, рекомендациями по питанию и отслеживанием привычек. Построен на Python с Redis кэшированием и интеграцией OpenRouter AI.

## 🏗️ Архитектура системы

### Высокоуровневая архитектура
```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Клиент    │────│  Telegram   │────│   Python    │────│   SQLite    │
│ (Telegram   │    │   Bot API   │    │     Бот     │    │   База      │
│   App)      │    │             │    │   Сервисы   │    │  Данных     │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
                         │                    │                    │
                         ▼                    ▼                    ▼
                 ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
                 │  Ограничение│    │  OpenRouter │    │   Redis     │
                 │   запросов  │    │     API     │    │   Кэш       │
                 │             │    │             │    │             │
                 └─────────────┘    └─────────────┘    └─────────────┘
```

## 🚀 Быстрый старт

### Предварительные требования
- Python 3.8+
- Redis Server
- Telegram Bot Token
- OpenRouter API Key

### Установка

1. **Клонируйте репозиторий**
```bash
git clone https://github.com/yourusername/ai-fitness-coach-bot.git
cd ai-fitness-coach-bot
```

2. **Настройте переменные окружения**
```bash
cp .env.example .env
# Отредактируйте .env с вашими данными
```

3. **Установите зависимости**
```bash
pip install -r requirements.txt
```

4. **Запустите бота**
```bash
python main.py
```

### Конфигурация окружения
```env
TELEGRAM_TOKEN=your_telegram_bot_token
OPENROUTER_API_KEY=your_openrouter_api_key
REDIS_URL=redis://localhost:6379
```

## 📁 Структура проекта

```
ai-fitness-coach-bot/
├── src/
│   ├── bot/
│   │   ├── __init__.py
│   │   ├── main.py              # Главный класс бота
│   │   ├── handlers/            # Обработчики команд Telegram
│   │   └── middleware/          # Rate limiting и др.
│   ├── services/
│   │   ├── ai_service.py        # Интеграция с OpenRouter
│   │   ├── redis_manager.py     # Redis кэширование
│   │   ├── workout_manager.py   # Планы тренировок
│   │   ├── recipe_manager.py    # Рецепты питания
│   │   └── habit_tracker.py     # Управление привычками
│   ├── models/
│   │   ├── user.py              # Модели пользователей
│   │   ├── workout.py           # Модели тренировок
│   │   └── conversation.py      # Модели чатов
│   └── utils/
│       ├── calculator.py        # Фитнес-расчеты
│       ├── formatters.py        # Форматирование ответов
│       └── validators.py        # Валидация ввода
├── tests/
├── docs/
├── requirements.txt
├── Dockerfile
├── docker-compose.yml
└── README.md
```

## 🔧 Основные компоненты

### 1. SmartAICoachBot (`src/bot/main.py`)
Главный класс бота, управляющий всеми сервисами и обрабатывающий обновления Telegram.

**Ключевые функции:**
- Инициализация бота Telegram и polling
- Маршрутизация команд и обработка сообщений
- Оркестрация сервисов
- Обработка ошибок и логирование

### 2. AIService (`src/services/ai_service.py`)
Интеграция с OpenRouter API для интеллектуальных бесед.

```python
class AIService:
    async def generate_response(self, user_id: int, message: str, context: Dict) -> str:
        """Генерация AI ответа с учетом контекста"""
```

### 3. RedisManager (`src/services/redis_manager.py`)
Слой Redis кэширования для оптимизации производительности.

**Стратегии кэширования:**
- Данные пользователя: срок 1 час
- Планы тренировок: срок 24 часа  
- Ответы AI: срок 5 минут
- История диалогов: срок 2 часа

### 4. EnhancedWorkoutManager (`src/services/workout_manager.py`)
Генерация персонализированных планов тренировок.

**Поддерживаемые уровни:**
- Начинающий: Базовые упражнения с весом тела
- Средний: Акцент на силовые тренировки
- Продвинутый: Оптимизация производительности

## 💾 Схема базы данных

### Таблицы SQLite

```sql
-- Таблица пользователей
CREATE TABLE users (
    user_id INTEGER PRIMARY KEY,
    goals TEXT,
    experience TEXT CHECK(experience IN ('beginner', 'intermediate', 'advanced')),
    available_time INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Таблица привычек  
CREATE TABLE habits (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    habit_name TEXT,
    frequency TEXT,
    completed BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users (user_id)
);
```

### Паттерны Redis ключей

```python
# Кэширование данных пользователя
f"user:{user_id}:data"           # Данные профиля
f"user:{user_id}:workout:{level}" # Планы тренировок
f"user:{user_id}:conversation"   # История чатов
f"ai_response:{user_id}:{hash}"  # Кэшированные ответы AI
f"rate_limit:{user_id}:{minute}" # Ограничение запросов
```

## 🤖 Доступные команды

| Команда | Описание | Пример |
|---------|-------------|---------|
| `/start` | Инициализация бота | `/start` |
| `/workout` | Получить тренировку | `/workout` |
| `/recipe` | Получить рецепты | `/recipe` |
| `/habits` | Отслеживание привычек | `/habits` |
| `/progress` | Просмотр прогресса | `/progress` |
| `/ai` | Чат с AI коучем | `/ai Как набрать массу?` |
| `/data` | Настройка профиля | `/data` |
| `/calculate` | Фитнес-расчеты | `/calculate bmi` |

## 🔌 Интеграции API

### OpenRouter API
- **Модель**: Anthropic Claude-3 Sonnet
- **Эндпоинт**: `https://openrouter.ai/api/v1/chat/completions`
- **Лимиты**: Управление через Redis кэширование
- **Резервный вариант**: Локальная генерация ответов при недоступности API

### Telegram Bot API
- **Фреймворк**: python-telegram-bot v20.0+
- **Функции**: Inline клавиатуры, форматирование сообщений, поддержка webhooks

## ⚡ Оптимизация производительности

### Стратегия кэширования
```python
# Многоуровневое кэширование
1. Кэш ответов Redis: 5 минут
2. Кэш тренировок Redis: 24 часа  
3. Кэш данных пользователя Redis: 1 час
4. Резервное хранилище в памяти: при недоступности Redis
```

### Ограничение запросов
- **Лимит**: 30 запросов в минуту на пользователя
- **Хранилище**: Счетчик на основе Redis
- **Резервный вариант**: Отслеживание в памяти при недоступности Redis

## 🐳 Деплоймент

### Деплоймент с Docker
```yaml
# docker-compose.yml
version: '3.8'
services:
  bot:
    build: .
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
```

### Ручной деплоймент
```bash
# Установите зависимости
pip install -r requirements.txt

# Установите переменные окружения
export TELEGRAM_TOKEN="your_token"
export OPENROUTER_API_KEY="your_key"

# Запустите бота
python src/bot/main.py
```

## 🧪 Тестирование

```bash
# Запустите unit тесты
pytest tests/

# Запустите с покрытием
pytest --cov=src tests/

# Протестируйте конкретный компонент
pytest tests/test_workout_manager.py
```

## 📊 Мониторинг и логирование

### Конфигурация логирования
```python
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('bot.log', encoding='utf-8'),
        logging.StreamHandler()
    ]
)
```

### Отслеживаемые метрики
- Время ответа на AI запросы
- Hit rate кэша
- Статистика вовлеченности пользователей
- Частота и типы ошибок

## 🔒 Безопасность

### Безопасность окружения
- API ключи хранятся в переменных окружения
- Отсутствие хардкодинга credentials
- Валидация и санитизация ввода

### Ограничение запросов
- Предотвращение злоупотребления API
- Защита от DDoS атак
- Обеспечение честного использования

## 🤝 Участие в разработке

1. Форкните репозиторий
2. Создайте ветку функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

### Настройка для разработки
```bash
# Установите зависимости для разработки
pip install -r requirements-dev.txt

# Настройте pre-commit хуки
pre-commit install

# Запустите тесты
pytest
```

## 📝 Лицензия

Этот проект лицензирован под MIT License - смотрите файл [LICENSE](LICENSE) для деталей.

## 🆘 Решение проблем

### Частые проблемы

1. **Ошибка подключения к Redis**
   - Проверьте запущен ли Redis сервер
   - Убедитесь в правильности Redis URL в переменных окружения

2. **Ошибки OpenRouter API**
   - Проверьте валидность API ключа
   - Убедитесь в наличии интернет-соединения
   - Проверьте лимиты API

3. **Невалидный Telegram Token**
   - Проверьте токен бота с @BotFather
   - Проверьте название переменной окружения

### Режим отладки
Включите debug логирование установкой:
```python
logging.getLogger().setLevel(logging.DEBUG)
```

## 📞 Поддержка

- Создайте [Issue](https://github.com/yourusername/ai-fitness-coach-bot/issues)
- Проверьте [Wiki](https://github.com/yourusername/ai-fitness-coach-bot/wiki) для документации
- Email: your-email@example.com
